# Future backend responsibilities

This delivery uses session-scoped browser mock data only. Before production, connect a server-backed repository and implement authenticated authorization, server-side schema validation, row-level access controls, secret management, rate limiting, audit logs, backups, retention/deletion controls, file type and malware checks, transactional email delivery, payment verification, idempotency, and monitoring. Never trust admin state or role claims from browser storage.
