'use client'; import { useEffect,useState } from 'react'; import { useParams } from 'next/navigation'; import { PackageForm } from '@/components/admin'; import type { Package } from '@/lib/domain'; export default function EditPackage(){const params=useParams<{id:string}>();const [item,setItem]=useState<Package|null>(null);useEffect(()=>{fetch(`/api/packages/${params.id}`).then(r=>r.json()).then(x=>setItem(x.data||null))},[params.id]);return item?<PackageForm initial={item}/>:<div className="p-10 text-center">Loading package…</div>}
