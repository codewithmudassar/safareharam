'use client'; import { useState } from 'react'; import toast from 'react-hot-toast'; import { AdminShell } from '@/components/admin'; import { settings } from '@/lib/mock-data';
export default function SettingsAdmin(){const [x,setX]=useState(settings);const up=(k:keyof typeof settings,v:string)=>setX(s=>({...s,[k]:v}));return <AdminShell title="Agency settings"><form onSubmit={e=>{e.preventDefault();toast.success('Settings saved for this admin session.')}} className="grid max-w-3xl gap-5 border border-border bg-ivory p-6 sm:grid-cols-2"><label className="grid gap-2 text-sm font-semibold">Agency name<input value={x.agencyName} onChange={e=>up('agencyName',e.target.value)} className="h-11 border border-border bg-white px-3 font-normal"/></label><label className="grid gap-2 text-sm font-semibold">Public phone<input value={x.phone} onChange={e=>up('phone',e.target.value)} className="h-11 border border-border bg-white px-3 font-normal"/></label><label className="grid gap-2 text-sm font-semibold">WhatsApp number<input value={x.whatsapp} onChange={e=>up('whatsapp',e.target.value)} className="h-11 border border-border bg-white px-3 font-normal"/></label><label className="grid gap-2 text-sm font-semibold">Email<input type="email" value={x.email} onChange={e=>up('email',e.target.value)} className="h-11 border border-border bg-white px-3 font-normal"/></label><label className="grid gap-2 text-sm font-semibold sm:col-span-2">Address<textarea value={x.address} onChange={e=>up('address',e.target.value)} className="min-h-24 border border-border bg-white p-3 font-normal"/></label><label className="grid gap-2 text-sm font-semibold sm:col-span-2">Tagline<textarea value={x.tagline} onChange={e=>up('tagline',e.target.value)} className="min-h-24 border border-border bg-white p-3 font-normal"/></label><div className="border-t border-border pt-5 sm:col-span-2"><button className="h-11 bg-primary px-6 text-sm font-bold text-white">Save settings</button></div></form></AdminShell>}
