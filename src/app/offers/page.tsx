import { offerRepo } from '@/lib/repository'; import { EditorialPage } from '@/components/content';
export default function Offers(){return <EditorialPage eyebrow="Current offers" title="Small details that make planning easier." copy="Sample offers are configurable in the admin workspace and should be confirmed before launch."><div className="grid gap-5">{offerRepo.list().map(x=><article key={x.id} className="border border-border bg-white p-7"><span className="text-xs font-bold uppercase tracking-wider text-gold-strong">{x.badge}</span><h2 className="mt-3 font-display text-3xl text-primary">{x.title}</h2><p className="mt-3 leading-7 text-muted">{x.description}</p><p className="mt-5 text-xs text-muted">Valid until {x.validUntil}</p></article>)}</div></EditorialPage>}
