import { EditorialPage } from '@/components/content';
const services=[['Journey planning','Understand your dates, priorities, room needs, and departure city.'],['Visa & documentation','Receive a clear checklist and practical support before submission.'],['Hotels & transfers','Travel with selected stays and coordinated airport and intercity movement.'],['Guided ziyarat','Make space for meaningful visits with informed local coordination.']];
export default function Services(){return <EditorialPage eyebrow="How we help" title="Support for every detail." copy="From the first conversation to your return transfer, each service is composed around ease and dignity."><div className="grid gap-6 sm:grid-cols-2">{services.map(([title,copy])=><article key={title} className="border border-border bg-white p-7"><span className="text-xs font-bold uppercase tracking-wider text-gold-strong">0{services.findIndex(x=>x[0]===title)+1}</span><h2 className="mt-5 font-display text-3xl text-primary">{title}</h2><p className="mt-3 text-sm leading-7 text-muted">{copy}</p></article>)}</div></EditorialPage>}
