import { NextResponse } from 'next/server'; import { packageRepo } from '@/lib/repository'; import { connectMongo,hasMongo } from '@/lib/mongodb'; import { PackageModel } from '@/models/mongoose';
const MongoPackage=PackageModel as any;
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(hasMongo){await connectMongo();const data=await MongoPackage.findOne({id}).lean();return data?NextResponse.json({success:true,data}):NextResponse.json({success:false,message:'Package not found'},{status:404})}const item=packageRepo.all().find(x=>x.id===id);return item?NextResponse.json({success:true,data:item}):NextResponse.json({success:false,message:'Package not found'},{status:404})}
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const body=await request.json();if(hasMongo){await connectMongo();const data=await MongoPackage.findOneAndUpdate({id},body,{new:true,runValidators:true}).lean();return data?NextResponse.json({success:true,data}):NextResponse.json({success:false,message:'Package not found'},{status:404})}const item=packageRepo.update(id,body);return item?NextResponse.json({success:true,data:item}):NextResponse.json({success:false,message:'Package not found'},{status:404})}
export async function DELETE(_request:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(hasMongo){await connectMongo();const result=await MongoPackage.deleteOne({id});return result.deletedCount?NextResponse.json({success:true,message:'Package deleted'}):NextResponse.json({success:false,message:'Package not found'},{status:404})}return packageRepo.remove(id)?NextResponse.json({success:true,message:'Package deleted'}):NextResponse.json({success:false,message:'Package not found'},{status:404})}
