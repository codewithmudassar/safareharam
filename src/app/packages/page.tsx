import { packageRepo } from '@/lib/repository'; import { PackageCard, PublicLayout, SectionHead } from '@/components/site';
export default function Packages(){const items=packageRepo.list();return <PublicLayout><section className="bg-primary px-5 py-20 text-primary-foreground sm:px-8 sm:py-28"><div className="mx-auto max-w-7xl"><p className="text-xs font-bold uppercase tracking-[.22em] text-gold">Selected journeys</p><h1 className="mt-4 max-w-3xl font-display text-5xl sm:text-7xl">Packages designed around ease.</h1><p className="mt-6 max-w-xl leading-7 text-white/70">Compare sample itineraries, then speak with our team for live availability and a tailored proposal.</p></div></section><section className="mx-auto max-w-7xl px-5 py-20 sm:px-8"><SectionHead eyebrow="Explore" title="Find your pace" copy="Every package includes coordinated support and clear pre-departure guidance."/><div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(item=><PackageCard key={item.id} item={item}/>)}</div></section></PublicLayout>}
